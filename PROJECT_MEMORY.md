# SuperGuardian 项目记忆

本文件记录项目中需要长期保留的事实、设计约束和开发约定。修改功能前先阅读本文件；当代码、构建方式或产品约束发生变化时，应同步更新这里，避免后续开发丢失上下文。

## 1. 项目定位

SuperGuardian（超级守护）是 Windows x64 原生桌面程序，用于守护和定时管理指定进程。

主要功能：

- 进程退出后自动重新启动。
- 定时重启和定时运行，支持周期、固定时间、星期和高级时间规则。
- 启动延时、失败重试和 SMTP 邮件通知。
- 看门狗自我守护、主程序未响应检测、MiniDump 和自动恢复。
- 托盘常驻、开机自启、明暗主题、列表排序和布局持久化。
- 配置导入/导出、诊断信息导出、本地更新、在线更新和旧版本恢复。

## 2. 技术与构建基线

- 语言：C++20。
- UI：Qt Widgets。
- Qt：6.11.0。
- 数据库：Qt SQL + SQLite。
- 平台：Windows x64。
- 工具集：MSVC v145。
- 解决方案：`SuperGuardian.slnx`。
- 工程文件：`project/SuperGuardian.vcxproj`。
- Debug 使用动态 Qt，并通过 `windeployqt` 部署运行库。
- Release 使用静态 Qt和静态 C/C++ 运行库，目标为单文件 `SuperGuardian.exe`。
- Debug/Release 输出分别位于 `x64/Debug/`、`x64/Release/`。
- 版本号以 `src/app/main.cpp` 和 `resources/app.rc` 为准，使用 `tools/set-version.ps1` 同步修改。
- 完整构建和打包使用 `tools/package.ps1`；已有 Release 产物仅打包使用 `tools/package-release.ps1`。
- 静态 Qt 构建脚本位于 `tools/build_static_qt.bat`。

当前记录快照为 1.0.8；不要依赖本行判断最新版本，修改或发布前应重新读取上述两个版本源文件。

## 3. 目录与模块职责

- `include/GuardItem.h`：守护项、时间规则、重试和邮件通知数据模型。
- `include/SuperGuardian.h`：主窗口类和跨模块接口。
- `src/app/main.cpp`：应用入口、单实例、看门狗模式分流和自动重启提示。
- `src/app/SuperGuardian.cpp`：主窗口创建、菜单、工具按钮、信号和初始化。
- `src/app/SuperGuardianProcess.cpp`：守护、定时重启、定时运行、延时和重试主循环。
- `src/app/SuperGuardianActions.cpp`：列表右键菜单及单项操作。
- `src/app/SuperGuardianBatchOps.cpp`：批量操作、添加程序和邮件事件分发。
- `src/config/SuperGuardianConfigImport.cpp`：配置导入、导出、重置和诊断信息。
- `src/storage/ConfigDatabase.cpp`：配置数据库、日志数据库及日志便捷接口。
- `src/system/ProcessUtils.cpp`：Windows 进程、快捷方式、启动项、看门狗和 Dump。
- `src/ui/`：对话框、规则编辑器、更新、白名单、列布局和窗口行为。
- `src/table/DesktopSelectTable.cpp`：表格选择、拖动、键盘操作和自定义绘制。
- `src/theme/ThemeManager.cpp`：主题检测和样式表。
- `tools/`：版本、构建和打包脚本。

当前主界面主要由 C++ 动态创建；`ui/SuperGuardian.ui` 和 `ui/SuperGuardian_fixed.ui` 没有被运行时代码直接使用。修改界面前先确认目标控件是否由 C++ 创建，不要只改 `.ui` 文件。

## 4. 核心运行模型

### 单实例

- 主程序使用名为 `SuperGuardianSingleton` 的 `QSharedMemory` 保证单实例。
- 再次启动时通过注册消息 `SuperGuardianShowMainWindow` 通知现有窗口显示。
- 更新或看门狗恢复使用 `--restart`，启动时等待旧实例释放单实例锁。

### 进程监控

- `checkProcesses()` 每 200 毫秒运行一次。
- 每轮统一调用 `takeProcessSnapshot()`，避免对每个守护项重复创建系统进程快照。
- 当前进程身份主要按不区分大小写的 EXE 文件名判断，不按完整路径或 PID 判断。
- `killProcessesByName()` 会终止所有同名进程。涉及同名程序时必须意识到这一限制，不要误以为只会操作配置路径对应的那个实例。

### 三类任务

- 守护：目标不在运行时自动启动。
- 定时重启：触发时终止同名进程，然后重新启动目标。
- 定时运行：触发时只启动，不终止已有进程。
- 守护与定时重启可以同时启用。
- 定时运行与守护、定时重启互斥。
- 每个任务还受对应的全局开关控制；导入配置后会主动关闭三个全局开关，避免立即批量启动任务。

### 时间规则

- `Periodic`：从当前时间按秒数递增。
- `FixedTime`：固定时间，可限制星期。
- `Advanced`：可限制年、月、日、星期、时、分、秒。
- 下一次触发时间保存在每条 `ScheduleRule::nextTrigger` 中。
- 同一轮中多条规则同时到期时只执行一次，并分别推进各自的下次触发时间。

## 5. 不可破坏的产品约束

### 守护启动延时与定时重启延时

这是明确的用户要求，优先级高于界面提示、历史注释和一般化重构：

1. 守护检测到进程退出后，如配置了启动延时，应开始倒计时。
2. 守护倒计时结束后，必须再次检查进程是否已经自行恢复运行。
3. 如果进程已经运行，守护不得再次启动它；如果仍未运行，才执行启动。
4. 定时重启选择使用启动延时时，倒计时结束后直接启动目标程序。
5. 定时重启的延时结束后不执行上述“已运行则取消启动”的二次检查。

对应状态字段：

- `startDelayExitTime`：守护退出后的非阻塞延时状态。
- `pendingScheduledRestartTime`：定时重启杀进程后的非阻塞延时状态。
- `restartUseStartDelay`：定时重启是否使用启动延时；默认关闭。

维护时不要把两条延时流程合并成完全相同的通用逻辑。

### 自我守护

- 看门狗通过同一个 EXE 的 `--watchdog <主进程 PID> <主程序路径>` 模式运行。
- 正常退出前写入 `self_guard_manual_exit`，看门狗不得恢复正常退出的主程序。
- 看门狗每秒检查主进程，并用 `WM_NULL` 检查主窗口响应。
- 连续 3 次超时会生成 MiniDump、终止主进程并记录 `restart_reason=hang`。
- 异常退出后最多尝试重启 5 次，失败时采用递增等待。
- Windows 关机、重启和注销不属于异常退出：主窗口收到 `WM_QUERYENDSESSION`/`WM_ENDSESSION` 后写入临时的 `system_shutdown_in_progress` 标记并停止看门狗。
- 看门狗在进程检查、生成 Dump 和执行重启前都必须同时检查 `SM_SHUTTINGDOWN` 与 `system_shutdown_in_progress`；任一成立时直接退出，不生成 Dump、不记录异常原因、不重启主程序。
- 如果 Windows 会话结束被取消，应清除临时标记并恢复原有自我守护状态；下次正常启动也应清理上次关机遗留的临时标记和错误通知状态。
- 修改主线程中的长时间操作时要继续处理事件，避免看门狗把正常工作误判为未响应。

### 更新

- 支持本地 `.exe`、本地 `.zip` 和 GitHub Release 在线更新。
- 更新前把当前 EXE 备份到 `bak/<时间戳>/SuperGuardian.exe.bak`。
- 最多保留 5 份历史备份。
- 更新完成后以 `--restart` 启动新程序，再退出当前实例。
- 在线更新来源固定为 `Mrluy/SuperGuardian` 的最新 GitHub Release。

## 6. 持久化与数据格式

- 应用数据位于程序同级 `data/` 目录，属于便携式存储，不使用用户 AppData。
- `data/config.db` 保存全部配置，表名为 `config`，键值以 JSON 包装后写入 SQLite。
- `data/logs.db` 保存操作、运行、守护、定时重启和定时运行日志。
- SQLite 使用 WAL 模式和 `synchronous=NORMAL`。
- 每类日志超过 100500 条时删除最早的 500 条，使其回到约 100000 条。
- 守护项集合在配置数据库的 `items` 键中以 JSON 数组字符串保存。
- 主循环约每 5 秒调用一次 `saveSettings()`，关键用户操作也会立即保存。
- 配置导出为结构化 JSON；导入时转换回内部扁平格式，支持增量或覆盖。
- 增量导入目前按规范化后的程序路径合并项目。

安全注意：SMTP 密码当前以明文保存在 `config.db`，也会明文写入导出的配置 JSON。处理配置文件、诊断材料或日志时不要泄露密码。

## 7. 日志与通知

日志类别：

- `operation`：用户操作。
- `runtime`：软件运行、看门狗和重试事件。
- `guard`：守护触发事件。
- `scheduled_restart`：定时重启事件。
- `scheduled_run`：定时运行事件。

邮件通知必须同时满足全局邮件开关和单项目邮件开关。失败类通知使用项目内状态位去重，成功运行后重置通知状态，避免持续发送相同失败邮件。

SMTP 发送通过 PowerShell 的 `System.Net.Mail` 完成；修改参数转义、编码或进程等待逻辑时，应同时验证测试邮件和异步通知两条路径。

## 8. 已知限制与文档偏差

- 进程检测与终止以 EXE 文件名为主，无法可靠区分路径不同但同名的程序。
- 在线更新目前没有额外的哈希或数字签名校验。
- SMTP 密码为明文存储和明文导出。
- 项目当前没有自动化测试和 CI，核心守护逻辑修改后必须进行人工运行验证。
- README 中的部分构建路径已过时：工程实际位于 `project/`，脚本实际位于 `tools/`。
- README 声称会自动迁移旧版 INI，但当前源码中没有对应的 INI 检测或迁移实现。除非补上实现，否则不要继续把该描述当成已实现功能。
- 定时重启对话框中“使用启动延时”的旧提示曾声称延时结束后会再次检查运行状态；这与用户约束不一致。实际行为和后续文案都应遵循第 5 节。
- `resources/SuperGuardian.qrc` 当前为空，实际资源位于 `resources/resources.qrc`。

## 9. 修改与验证约定

- 修改前先阅读 `AGENTS.md`、本文件和 `.github/copilot-instructions.md`。
- 保留工作区中与当前任务无关的用户修改，不要执行破坏性的 Git 清理命令。
- 修改核心监控逻辑时，至少验证以下场景：
  - 守护目标已退出且延时为 0。
  - 守护目标退出、进入延时、随后自行恢复，确认不会重复启动。
  - 定时重启不使用延时。
  - 定时重启使用延时，确认倒计时结束后按产品约束直接启动。
  - 启动失败进入重试、重试成功、次数或时长耗尽。
  - 守护与定时重启同时启用时的 30 秒冷却逻辑。
  - 三个全局功能开关暂停和恢复任务。
- 修改自我守护或耗时 UI 操作时，验证不会触发错误的未响应重启。
- 修改看门狗或窗口生命周期时，验证 Windows 关机/重启不会生成 Dump、记录异常退出或启动新实例，并验证取消关机后看门狗能够恢复。
- 修改持久化字段时，应同步处理：默认值、加载、保存、结构化导出、导入转换和诊断导出。
- 修改版本时使用 `tools/set-version.ps1`，并核对 `main.cpp` 与 `app.rc` 一致。
- Release 构建会在预构建阶段清理输出目录；在用户已有产物可能需要保留时，不要未经确认直接构建。
- 完成修改后检查 `git status` 和目标文件差异，只提交当前任务范围内的文件。
- 每次修改并完成必要验证后必须创建本地 Git 提交；提交前检查暂存差异，确保不混入无关文件。
- Git 提交说明统一使用中文。
- 默认只保留本地提交，不推送任何远端；只有用户在当前任务中明确要求时才允许执行 push。
- 未经用户明确要求，不创建 Release 或执行其他发布操作。

## 10. 记忆维护规则

- 本文件记录长期有效的事实和约束，不记录临时调试过程。
- 新增重要配置字段、核心行为、发布规则或用户明确要求时，应同步更新本文件。
- 如果本文件与实际代码不一致，先查明是代码回归还是记忆过期；涉及第 5 节的明确用户约束时，以该约束为准。
- `.github/copilot-instructions.md` 和 `AGENTS.md` 应保持简短，统一指向本文件，避免三处维护相互冲突的完整副本。
